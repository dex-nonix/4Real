[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "talki_app"
version = "0.1.0"
description = "UI-first Talki app (bundles STT engine internally)."
readme = "README.md"
authors = [{name = "You"}]
dependencies = [
  "numpy",
  "colorama",
  "sounddevice",
  "librosa",
  "faster-whisper",
  "torch",
  "transformers",
  "tokenizers",
  "ctranslate2",
  "PyQt6",
  "qasync",
  "pynput",
  "mouse"
]

[project.scripts]
talki-app = "talki_app.__main__:main"

[tool.setuptools]
packages = ["talki_app"]

[tool.setuptools.package-dir]
talki_app = "."


